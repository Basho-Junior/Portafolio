#include <stdio.h>
#include <stdlib.h>
#define MAXELEM 10
void caparr();
void imparr;
int main()
{
    /*int numeros[MAXELEM] = {0}, ind;
    printf("Indique los valores: \n");
    for (ind = 0; ind < MAXELEM; ind ++){
        printf("Valor[%d]: ",ind);
        scanf("%d", &numeros[ind]);
    }
    for (ind = 0; ind < MAXELEM; ind++){
    printf("[%d]", numeros[ind]);
    }*/

    return 0;
}


